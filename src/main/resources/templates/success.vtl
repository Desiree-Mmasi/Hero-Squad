#if ($hero)
#foreach ($hero in $hero)

<div class="container>
    <div class="card-deck">
          <div class="card-body">

  <p class="card-text" style="font-family: 'Poiret One', cursive;"><strong>$hero.getName()</strong></p>
  <p class="card-text" style="font-family: 'Poiret One', cursive;"><strong>$hero.getAge()</strong></p>
  <p class="card-text" style="font-family: 'Poiret One', cursive;"><strong>$hero.getPower()</strong></p>
  <p class="card-text" style="font-family: 'Poiret One', cursive;"><strong>$hero.getWeakness()</strong></p>
      </div>
   </div>
   </div>

   #end
   <br>
   #else
   <p style="font-family: 'Poiret One', cursive;"><strong>Please Input your Avatar</strong></p>
#end